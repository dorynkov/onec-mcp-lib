[project]
name = "onec-mcp-lib"
version = "0.1.0"
description = "Shared utilities for 1c-mcp-* — tool_safe, logging, health base"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
